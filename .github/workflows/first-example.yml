name: my first workflow

on: push

jobs:
    my_first_job:
        runs-on: ubuntu-latest
        steps:
        - name: welcome message
          run: echo "Hello, this is first workflow run"

        - name: list of files
          run: ls

        - name: Read the file
          run: cat README.md
